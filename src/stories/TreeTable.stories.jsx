import React from 'react';

import { TreeTable } from './../TreeTable';

export default {
  title: 'Tree Table Story',
  component: TreeTable,
};

const Template = () => <TreeTable />;

export const TreeTableStory = Template.bind({});
